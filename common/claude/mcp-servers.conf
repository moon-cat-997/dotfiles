# User-scope MCP servers restored by claude-sync (common/claude/claude-sync.sh).
# Format: <name> <url> — one per line, all HTTP transport.
# Auth is OAuth, granted lazily on first use inside Claude Code — never stored here.
base44   https://app.base44.com/mcp
context7 https://mcp.context7.com/mcp
Jam      https://mcp.jam.dev/mcp
mobbin   https://api.mobbin.com/mcp
